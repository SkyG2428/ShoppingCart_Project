@model IEnumerable<DBFirstLayer.Models.Fee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FeesAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Class)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FeesAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Class.ClassId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FeesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FeesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FeesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
